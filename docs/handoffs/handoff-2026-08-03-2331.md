# Handoff — 2026-08-03 23:31 (Taipei) — Retina resolution fixes + full-site verification

**Read [handoff-2026-08-03-2246.md](handoff-2026-08-03-2246.md) FIRST** — it covers the
bulk of today: standalone migration, IM Creator purge, blur fixes #1/#2, revamp plan.
This brief covers only what happened AFTER that wrap.

## 1. Mission
Same as previous brief: standalone portfolio at max fidelity, foundation for Ceci's
revamp (new homepage + 3 pages, built on this Mac with this Claude Code).

## 2. Current State — DEPLOYED, VERIFIED, RESOLUTION CLOSED
- `main` @ (this wrap's commit), in sync with origin. Live deploy green.
- **Image resolution is now a CLOSED topic**: every image on all 25 pages serves the
  highest rendition that exists anywhere (verified file-by-file vs Google `=s0`).
- Repo ~171 MB (160 MB images). Site families: base→`=s0` originals (to 6300px),
  s300-family→`=s2600`, small variants untouched.

## 3. Completed This Session (post-2246 only)
1. **Blur fix #3 — retina backgrounds** (commit `68abb47`): live measurement at dpr=2
   found bnct gallery bgs at 1600px vs 1998–2560 need (ratio 0.63–0.80). All 184
   s300-family files upgraded =s1600→=s2600. Google never upscales, so 57 hit 2600,
   127 capped at their true original.
2. **Deploy race caught**: the 68abb47 Pages run FAILED (transient GitHub 503) while
   the wait-loop saw the PREVIOUS run's "completed" — production kept serving old
   bytes under a green verdict. Fixed by resolving the run ID for the exact SHA,
   `gh run rerun`, then verifying served bytes (curl + sips = 2600px).
3. **Full-site retina sweep (verification, no code change)**: all 25 live pages
   measured in-browser at dpr=2, intrinsic vs displayed×2 for every img + bg.
   Result: **0 fixable deficits**. 19 flagged files classified via manifest→`=s0`
   comparison: ALL at source maximum (Ceci's uploads: 128–1800px). Old site showed
   same or worse (its runtime upsizer capped at 1600).
4. Docs: CLAUDE.md image rules updated (=s2600, source-limited list, DPR-2 check);
   global ~/.claude/CLAUDE.md gained the run-ID-gating trap + DPR≥2 verification rule.

## 4. In-Flight Work
None. Clean tree at wrap. All fixes deployed and content-verified on production.

## 5. Next Steps
1. **Ceci's revamp** — unchanged from previous brief §5. Add: her re-exports should be
   2× display size; the ~19 source-limited legacy images only get sharper if she
   re-exports them too (list re-derivable by re-running the retina sweep).
2. Optional: delete merged `feat/standalone-assets` branch (needs Albert's hand).
3. Optional: Google-Fonts pruning (perf).

## 6. Key Decisions + Rationale
- s300-family target = `=s2600`: covers worst measured need (2560 = 1280 CSS @2×).
  Not =s0 for this family: bandwidth (galleries load many at once) and 2600 already
  ≥ every possible display at current layouts.
- Source-limited files = WONTFIX with evidence, not bugs. Chasing them wastes time;
  only new exports from Ceci can improve them.

## 7. Open Questions / Blockers
None technical. Waiting on Ceci's revamp materials (see previous brief).

## 8. Gotchas & Landmines (new this session)
- **`gh run list --limit 1` is a RACE when gating deploys** — resolve the run ID for
  your SHA, poll that, check `conclusion`, then verify SERVED BYTES. (Now in global
  CLAUDE.md; bit us once already.)
- **Retina need is invisible at 1× viewports** — measure intrinsic vs displayed×DPR
  at dpr=2. The harness pattern: iframe per page, harvest computed backgroundImage +
  imgs at several scroll positions, Image() probe for intrinsic width. Hidden browser
  pane throttles timers — keep per-call work under ~25s, 2–3 pages per call.
- Browser cache mimics regressions after same-name content swaps — Albert may still
  see stale images until hard-refresh (Cmd+Shift+R); server verified correct.
- GitHub Pages 503s happen; `gh run rerun <id>` fixes without a new commit.

## 9. Files Modified (post-2246)
- `site/assets/img/*` — 184 s300-family files → =s2600 content (in place).
- `docs/asset-manifest.json` — s300 target note updated.
- `CLAUDE.md` — =s2600 + source-limited/DPR-2 rules.
- `docs/handoffs/handoff-2026-08-03-2331.md` — this brief.
- `~/.claude/CLAUDE.md` (not in repo) — run-ID gating trap; DPR≥2 + AT-SOURCE-MAX
  additions to the de-vendoring block.

## 10. Env / Config / Dependency changes
None.

## 11. Commands to Resume
```bash
cd ~/Documents/ClaudeCodex/ceci-portfolio && git pull --ff-only
python3 -m http.server 8765 --directory site   # local preview
```

## 12. Context the Summary Would Lose
- The retina sweep harness lives only in this transcript (throwaway JS via the
  browser pane). If needed again, rebuild from §8's description — or better,
  commit a small `verify_retina.py` (Playwright) during the revamp so the gate is
  runnable headlessly. Almost-decision: wasn't committed this time because the
  browser-pane version was interactive/throwaway.
- s300-family and base-family are SEPARATE files for the same source image (different
  hash suffixes); both were verified; don't "dedupe" them — HTML references both.
- Albert asked twice about resolution before the true root causes were all found —
  the sequence was: 300px thumbs frozen (=s1600 fix) → 512px bare-URL defaults
  (=s0 fix) → retina bg deficit (=s2600 fix) → sweep proving the rest is
  source-limited. Each layer masked the next. If he reports softness again: FIRST
  ask hard-refresh, THEN measure at dpr=2, THEN classify vs =s0 before touching
  anything.
